{
    "name": "demo-service",
    "private": true,
    "scripts": {
        "dev": "PORT=${PORT:-8080} npx nodemon server.mjs",
        "prod": "NODE_ENV=production PORT=${PORT:-80} node server.mjs"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.1",
        "mariadb": "^3.2.0"
    },
    "devDependencies": {
        "nodemon": "^2.0.19"
    }
}
